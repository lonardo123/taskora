name = "taskora"
main = "server.js"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# إعداد المهمة المجدولة (بديلاً عن setInterval في server.js)
[triggers]
crons = ["* * * * *"] # تشغيل كل دقيقة
